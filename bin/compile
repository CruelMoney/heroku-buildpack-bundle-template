#!/usr/bin/env bash

set -e

build=$1
cache=$2

mkdir -p $build
mkdir -p $cache

echo $build
cd $build

# git clone https://github.com/openvenues/libpostal --depth 1
# cd libpostal/
# ./bootstrap.sh
# ./configure --prefix=/app/libpostal --datadir=$build 

# make -j4
# make install

echo $build
